// Pointer to object field.cpp: определяет точку входа для консольного приложения.
//

#include "stdafx.h"

class A //оголошення класу
{
public:
	int i;
	int f(int j) { return 2 * j; }
};

void main() {
	A a;
	//оголошення вказівника на поле об’єкта
	int* p1 = &a.i;
	// заборонений вказівник на метод класу
	// int (*p2)(int)=&a.f;
	// заборонений виклик метода через вказівник
	// p2(3);
	// дозволене звертання до члена даних класу
	// через вказівник
	*p1 = 100;
}
